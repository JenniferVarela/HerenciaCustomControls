<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="CustomControls.MainPage"
             xmlns:controls="clr-namespace:CustomControls" >

    <StackLayout Margin="10,20">

        <controls:FromEntry Text="{Binding FirstName}" Title="First Name:"/>

        <controls:FromEntry Text="{Binding LastName}" Title="Last Name:"/>

        <controls:FromEntry Text="{Binding Company}" Title="Company:"/>
    </StackLayout>

</ContentPage>
